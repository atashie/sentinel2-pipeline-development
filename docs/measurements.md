# Measurements

No measurement exists on 2026-09-09. Nothing in this repository has contacted a provider.

When a benchmark runs, each row here names its evidence file in [../benchmarks/results/](../benchmarks/results/) and [../benchmarks/README.md](../benchmarks/README.md) says how to rerun it.
Every row states what the number is: bytes transferred, requests, pixels read, or output bytes. Every row states the distinct tiles and tile-dates it touched.
